/**
 * Payout balance UI was merged into `earnings-payments-overview` (Balance card on `/dashboard/earnings`).
 * This empty module exists so stale Tailwind/Turbopack content tracking that still resolves the old path
 * does not throw ENOENT. Safe to remove after a full cache clear and dev server restart.
 */
export {}
